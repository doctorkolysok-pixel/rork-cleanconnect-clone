# 🚀 Как правильно запустить приложение TazaGo

## ⚠️ Проблема: "Auth error: TRPCClientError: Failed to fetch"

Эта ошибка означает, что **бэкенд-сервер не запущен** или недоступен.

## ✅ Правильный порядок запуска

### Способ 1: Автоматический (когда работает)

Если команда `bun run start` запускает бэкенд автоматически:

```bash
bun run start
```

Но если вы видите ошибку "Failed to fetch", значит бэкенд не запустился. Переходите к **Способу 2**.

---

### Способ 2: Ручной запуск (Рекомендуется для отладки)

Нужно запустить **ДВА** процесса в **ДВУХ РАЗНЫХ** терминалах:

#### **Терминал 1: Запуск бэкенда**

```bash
bun server/index.ts
```

Вы должны увидеть:
```
🚀 Starting server on port 3000...
✅ Server running at http://localhost:3000
📡 tRPC endpoint: http://localhost:3000/api/trpc
🏥 Health check: http://localhost:3000/
```

**ВАЖНО:** Оставьте этот терминал открытым! Бэкенд должен работать постоянно.

#### **Терминал 2: Запуск фронтенда**

```bash
bun run start
```

Expo запустит приложение и вы увидите QR-код для мобильного устройства.

---

## 🔍 Проверка работы бэкенда

После запуска бэкенда, откройте в браузере:

```
http://localhost:3000/
```

Вы должны увидеть:
```json
{"status":"ok","message":"API is running"}
```

Или используйте скрипт проверки:

```bash
bun check-backend.ts
```

---

## 🗄️ Настройка базы данных (первый запуск)

Если вы запускаете приложение **первый раз**, выполните:

```bash
# 1. Генерация схемы БД
bun drizzle-kit generate

# 2. Применение миграций
bun drizzle-kit migrate

# 3. Заполнение демо-данными (опционально)
bun backend/db/seed.ts
```

После этого можно запускать бэкенд и фронтенд.

---

## 📱 Тестирование авторизации

После успешного запуска:

1. Откройте приложение
2. Перейдите на экран авторизации  
3. Попробуйте:
   - **Регистрация** - создайте новый аккаунт
   - **Вход** - если выполнили seed, используйте:
     - Телефон: `+77771111111`

---

## 🔧 Решение проблем

### Проблема 1: "Failed to fetch" после запуска

**Причина:** Бэкенд не запущен или недоступен

**Решение:**
```bash
# Проверьте доступность
curl http://localhost:3000/

# Если не работает, перезапустите бэкенд
# Остановите текущий процесс (Ctrl+C)
bun server/index.ts
```

---

### Проблема 2: Порт 3000 занят

**Решение:** Измените порт

```bash
PORT=3001 bun server/index.ts
```

И обновите переменную окружения `EXPO_PUBLIC_RORK_API_PORT=3001`

---

### Проблема 3: Ошибки базы данных

**Ошибка:** `no such table: users` или подобные

**Решение:**
```bash
# Удалите старую БД
rm tazago.db

# Создайте заново
bun drizzle-kit generate
bun drizzle-kit migrate
bun backend/db/seed.ts
```

---

### Проблема 4: Подключение с телефона

Если на телефоне появляется "Failed to fetch":

1. Убедитесь что бэкенд запущен на компьютере
2. Телефон и компьютер в одной Wi-Fi сети
3. Используйте флаг `--tunnel` в команде start (уже есть)
4. Проверьте файрвол - разрешите порт 3000

---

## 📊 Архитектура приложения

```
┌─────────────────────┐
│   Фронтенд          │
│   (React Native)    │  ← Вы видите интерфейс
│   bun run start     │
└──────────┬──────────┘
           │
           │ HTTP/tRPC
           ↓
┌─────────────────────┐
│   Бэкенд            │
│   (Hono + tRPC)     │  ← Обрабатывает запросы
│   bun server/...    │
└──────────┬──────────┘
           │
           │ SQL
           ↓
┌─────────────────────┐
│   База данных       │
│   (SQLite)          │  ← Хранит данные
│   tazago.db         │
└─────────────────────┘
```

**Для работы приложения нужны ОБА компонента: фронтенд И бэкенд!**

---

## 💡 Рекомендации

1. **Всегда** сначала запускайте бэкенд, потом фронтенд
2. Держите бэкенд запущенным в отдельном терминале
3. Используйте `bun check-backend.ts` для диагностики
4. При изменениях в БД не забывайте про миграции

---

## 📚 Дополнительные документы

- `BACKEND_START.md` - Подробная документация по бэкенду
- `DATABASE_GUIDE.md` - Работа с базой данных
- `QUICK_START_DB.md` - Быстрый старт с БД
- `ИСПРАВЛЕНИЕ_ОШИБКИ_AUTH.md` - История исправления ошибки

---

## ✅ Чеклист запуска

- [ ] База данных создана (`bun drizzle-kit migrate`)
- [ ] Бэкенд запущен (`bun server/index.ts`)
- [ ] Бэкенд отвечает на `http://localhost:3000/`
- [ ] Фронтенд запущен (`bun run start`)
- [ ] Приложение открыто на устройстве
- [ ] Авторизация работает без ошибок

Если все пункты выполнены, приложение должно работать! 🎉
