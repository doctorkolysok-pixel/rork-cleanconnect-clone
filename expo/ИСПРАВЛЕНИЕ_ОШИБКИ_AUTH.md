# ⚠️ Исправление ошибки "Auth error: TRPCClientError: Failed to fetch"

## 🎯 Проблема

Ошибка `TRPCClientError: Failed to fetch` при авторизации означает, что **бэкенд-сервер не запущен**.

## ✅ Что исправлено

1. ✅ Добавлен правильный HTTP-сервер в `server/index.ts`
2. ✅ Установлен пакет `@hono/node-server` для запуска сервера
3. ✅ Настроено логирование при старте сервера
4. ✅ Создана документация по запуску
5. ✅ Добавлено информативное сообщение об ошибке в приложении
6. ✅ Создан скрипт проверки бэкенда (`check-backend.ts`)
7. ✅ Создана подробная инструкция (`ЗАПУСК_ПРИЛОЖЕНИЯ.md`)

## 🚀 Как запустить приложение ПРАВИЛЬНО

### Способ 1: Автоматический запуск (Рекомендуется)

```bash
bun run start
```

Эта команда запустит:
- ✅ Бэкенд-сервер (порт 3000)
- ✅ Expo dev server
- ✅ Туннелирование

### Способ 2: Ручной запуск (для отладки)

**Терминал 1 - Запуск бэкенда:**
```bash
bun server/index.ts
```

Вы должны увидеть:
```
🚀 Starting server on port 3000...
✅ Server running at http://localhost:3000
📡 tRPC endpoint: http://localhost:3000/api/trpc
🏥 Health check: http://localhost:3000/
```

**Терминал 2 - Запуск фронтенда:**
```bash
bun run start
```

## 🔍 Проверка работы

1. **Откройте в браузере:** http://localhost:3000/
   - Должно показать: `{"status":"ok","message":"API is running"}`

2. **Если база данных пустая**, выполните:
```bash
# Генерация миграций
bun drizzle-kit generate

# Применение миграций
bun drizzle-kit migrate

# Заполнение демо-данными
bun backend/db/seed.ts
```

## 📱 Тестирование авторизации

После запуска бэкенда:

1. Откройте приложение
2. Перейдите на экран авторизации
3. Попробуйте:
   - **Регистрацию** - создайте новый аккаунт
   - **Вход** - войдите с телефоном: `+77771111111` (если выполнили seed)

## ⚠️ Если ошибка всё ещё есть

1. **Проверьте, что бэкенд запущен:**
```bash
curl http://localhost:3000/
```

2. **Проверьте порт:**
   - Убедитесь что порт 3000 свободен
   - Или измените порт: `PORT=3001 bun server/index.ts`

3. **Перезапустите всё:**
```bash
# Остановите все процессы (Ctrl+C)
# Затем запустите заново
bun run start
```

4. **Очистите кэш:**
```bash
bunx expo start --clear
```

## 📚 Дополнительная документация

- **Полная инструкция по бэкенду:** `BACKEND_START.md`
- **Работа с базой данных:** `DATABASE_GUIDE.md`
- **Быстрый старт с БД:** `QUICK_START_DB.md`

## 💡 Важно понимать

Ваше приложение состоит из двух частей:
1. **Фронтенд** (React Native приложение) - показывает интерфейс
2. **Бэкенд** (Node.js сервер) - обрабатывает данные и БД

Для работы авторизации **ОБЯЗАТЕЛЬНО** нужны оба компонента!

Команда `bun run start` запускает оба автоматически.
