# 🚀 Запуск бэкенда TazaGo

## Быстрый старт (3 шага)

### 1️⃣ Создать базу данных
```bash
bun backend/db/migrate.ts
```

### 2️⃣ Запустить сервер
```bash
bun server/index.ts
```

### 3️⃣ Запустить приложение (в другом терминале)
```bash
bun run start
```

---

## Проверка работы

### Проверить работу API
```bash
curl http://localhost:3000
```

**Ожидаемый результат:**
```json
{"status":"ok","message":"API is running"}
```

---

## Решение проблем

### ❌ Ошибка: "Failed to fetch"
**Причина:** Бэкенд не запущен

**Решение:**
1. Откройте новый терминал
2. Выполните: `bun server/index.ts`
3. Дождитесь сообщения "✅ Сервер успешно запущен!"
4. Попробуйте снова в приложении

### ❌ Ошибка: "Пользователь не найден"
**Причина:** База данных не создана или пуста

**Решение:**
1. Выполните: `bun backend/db/migrate.ts`
2. Перезапустите сервер
3. Зарегистрируйтесь заново в приложении

### ❌ Ошибка: "Cannot find module"
**Причина:** Не установлены зависимости

**Решение:**
```bash
bun install
```

---

## Что делает каждая команда?

1. **`bun backend/db/migrate.ts`** - Создает базу данных SQLite и все таблицы
2. **`bun server/index.ts`** - Запускает HTTP сервер с API на порту 3000
3. **`bun run start`** - Запускает React Native приложение с Expo

---

## Архитектура

```
Frontend (React Native + Expo)
    ↓
tRPC Client (lib/trpc.ts)
    ↓
HTTP Request → http://localhost:3000/api/trpc
    ↓
Backend (Hono + tRPC)
    ↓
Database (SQLite via Drizzle ORM)
```

---

## Порты

- **3000** - Backend API (tRPC)
- **8081** - Expo Dev Server (автоматически)

---

## Важно! 🔥

- Держите терминал с backend ОТКРЫТЫМ во время работы
- Backend должен быть запущен ДО запуска приложения
- Если что-то не работает - перезапустите backend

---

## Команды для разработки

```bash
# Проверить статус backend
bun check-backend.ts

# Остановить backend
Ctrl + C в терминале с сервером

# Пересоздать базу данных
rm tazago.db
bun backend/db/migrate.ts
```
