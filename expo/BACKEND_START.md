# 🚀 Запуск бэкенда

## Проблема: "TRPCClientError: Failed to fetch"

Эта ошибка означает, что бэкенд-сервер не запущен или недоступен.

## ✅ Решение: Запустите бэкенд сервер

### Вариант 1: Через Rork команду (Рекомендуется)

```bash
# Запуск приложения с бэкендом
bun run start
```

Команда `bun run start` автоматически запускает:
- ✅ Бэкенд-сервер на порту 3000
- ✅ Expo dev server
- ✅ Туннелирование для доступа с телефона

### Вариант 2: Запуск только бэкенда

Если вам нужно запустить только бэкенд сервер:

```bash
bun server/index.ts
```

Сервер запустится на `http://localhost:3000` и вы увидите:
```
🚀 Starting server on port 3000...
✅ Server running at http://localhost:3000
📡 tRPC endpoint: http://localhost:3000/api/trpc
🏥 Health check: http://localhost:3000/
```

### Вариант 3: Для веб-превью

```bash
bun run start-web
```

## 🔍 Проверка работы бэкенда

После запуска проверьте:

1. **Health check**: Откройте в браузере `http://localhost:3000/`
   - Должен вернуть: `{"status":"ok","message":"API is running"}`

2. **tRPC endpoint**: `http://localhost:3000/api/trpc`

## 📝 Первый запуск с базой данных

Если вы запускаете проект первый раз, выполните:

```bash
# 1. Установить зависимости
bun install

# 2. Сгенерировать миграции базы данных
bun drizzle-kit generate

# 3. Применить миграции
bun drizzle-kit migrate

# 4. Заполнить демо-данными
bun backend/db/seed.ts

# 5. Запустить приложение с бэкендом
bun run start
```

## ⚠️ Частые проблемы

### 1. "Failed to fetch" всё ещё появляется

**Причины:**
- Бэкенд не запущен
- Неправильный URL в конфигурации
- Проблема с сетью/портом

**Решение:**
```bash
# Убедитесь что бэкенд запущен и работает
bun server/index.ts

# В другом терминале проверьте доступность
curl http://localhost:3000/
```

### 2. Порт 3000 занят

**Решение:** Измените порт
```bash
PORT=3001 bun server/index.ts
```

И обновите `EXPO_PUBLIC_RORK_API_BASE_URL` в вашем окружении.

### 3. База данных не найдена

**Решение:** Выполните миграции
```bash
bun drizzle-kit generate
bun drizzle-kit migrate
```

## 🎯 Рекомендуемый workflow

Для разработки используйте два терминала:

**Терминал 1 - Запуск всего приложения:**
```bash
bun run start
```

Это запустит и бэкенд, и фронтенд одновременно.

## 📚 Дополнительная информация

- Полная документация по БД: `DATABASE_GUIDE.md`
- Быстрый старт с БД: `QUICK_START_DB.md`
- Список TODO для БД: `TODO_DATABASE.md`
