# 🎯 КАК ЗАПУСТИТЬ ПРИЛОЖЕНИЕ - ПРОСТАЯ ИНСТРУКЦИЯ

## 📋 3 ШАГА ДЛЯ ЗАПУСКА:

### ШАГ 1: Создай базу данных
Открой терминал и выполни:
```bash
bun backend/db/migrate.ts
```

Ты увидишь:
```
✅ База данных успешно инициализирована!
✅ Все таблицы созданы!
```

---

### ШАГ 2: Запусти backend сервер
В том же терминале выполни:
```bash
bun server/index.ts
```

Ты увидишь:
```
✅ Сервер успешно запущен!
🔗 Адреса:
   • Основной: http://localhost:3000
   • tRPC API: http://localhost:3000/api/trpc
```

**⚠️ ВАЖНО: НЕ ЗАКРЫВАЙ ЭТОТ ТЕРМИНАЛ!** Backend должен работать постоянно.

---

### ШАГ 3: Запусти приложение
**Открой НОВЫЙ терминал** (первый оставь открытым!) и выполни:
```bash
bun run start
```

Приложение запустится! 🎉

---

## 🔍 ПРОВЕРКА РАБОТЫ:

Если хочешь проверить, работает ли backend:
```bash
bun check-backend.ts
```

Если всё ок, увидишь:
```
✅ Backend работает!
✅ ВСЁ РАБОТАЕТ КОРРЕКТНО!
```

---

## ❌ ЕСЛИ ПОЯВИЛАСЬ ОШИБКА "Failed to fetch"

### Причина: Backend не запущен

### Решение:
1. Открой новый терминал
2. Выполни: `bun server/index.ts`
3. Дождись сообщения "✅ Сервер успешно запущен!"
4. Попробуй войти в приложение снова

---

## ❌ ЕСЛИ ПОЯВИЛАСЬ ОШИБКА "Пользователь не найден"

### Причина: База данных пуста или не создана

### Решение:
1. Останови backend (нажми Ctrl+C в терминале с backend)
2. Удали старую базу: `rm tazago.db`
3. Создай новую: `bun backend/db/migrate.ts`
4. Запусти backend: `bun server/index.ts`
5. Зарегистрируйся в приложении заново

---

## 💡 ПОЛЕЗНЫЕ КОМАНДЫ:

```bash
# Проверить, работает ли backend
bun check-backend.ts

# Создать/пересоздать базу данных
bun backend/db/migrate.ts

# Запустить backend
bun server/index.ts

# Запустить приложение
bun run start

# Остановить backend
Ctrl + C в терминале с backend
```

---

## 🎓 ЧТО ПРОИСХОДИТ ПРИ ЗАПУСКЕ?

1. **База данных** (`bun backend/db/migrate.ts`)
   - Создается файл `tazago.db`
   - Создаются таблицы для пользователей, заказов, сообщений и т.д.

2. **Backend** (`bun server/index.ts`)
   - Запускается HTTP сервер на порту 3000
   - Подключается к базе данных
   - Готов принимать запросы от приложения

3. **Приложение** (`bun run start`)
   - Запускается React Native приложение
   - Подключается к backend на http://localhost:3000
   - Готово к использованию!

---

## 🔥 ВАЖНЫЕ ПРАВИЛА:

1. ✅ Сначала ВСЕГДА запускай backend, потом приложение
2. ✅ Backend должен работать всё время, пока ты используешь приложение
3. ✅ Используй ДВА терминала: один для backend, другой для приложения
4. ✅ Если что-то не работает - перезапусти backend

---

## 🆘 НУЖНА ПОМОЩЬ?

Если что-то не работает:

1. Проверь, запущен ли backend: `bun check-backend.ts`
2. Перезапусти backend: Ctrl+C → `bun server/index.ts`
3. Пересоздай базу: `rm tazago.db` → `bun backend/db/migrate.ts`

---

## ✅ ГОТОВО!

Теперь у тебя:
- ✅ Работающий backend с базой данных
- ✅ Настроенная авторизация
- ✅ Рабочее приложение TazaGo

**Удачи! 🚀**
